// src/components/NotFound.jsx
const NotFound = () => {
  return (
    <div
      style={{
        padding: "40px",
        textAlign: "center",
        color: "#fff",
        backgroundColor: "#111",
        minHeight: "100vh",
      }}
    >
      <h2>Error 404</h2>
      <p>La página que buscas no existe.</p>
    </div>
  );
};

export default NotFound;
